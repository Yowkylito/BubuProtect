<?xml version="1.0" encoding="utf-8"?>
<!--
    API 31+. Nothing leaves the device - not to cloud backup, and not over
    device-to-device transfer. The encrypted DB is worthless without the
    Keystore key, which is non-exportable and stays on the original device,
    but excluding it outright removes the offline-attack surface entirely.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </device-transfer>
</data-extraction-rules>
